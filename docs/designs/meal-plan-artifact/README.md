# Meal Plan Artifact — Claude Design export

Raw prototype handoff from Claude Design (claude.ai/design), copied here for
reference. Not implemented yet.

- `Meal Plan Artifact.dc.html` — the prototype (HTML/CSS/JS), imports `support.js`
- `support.js` — supporting script the prototype depends on

See `project/github.md` and `chats/chat1.md` in the original handoff bundle
for the design intent and the repo screens it maps to
(`priv/corpus/README.md`, `cli/src/corpus.rs`, `priv/corpus/household.md`).
